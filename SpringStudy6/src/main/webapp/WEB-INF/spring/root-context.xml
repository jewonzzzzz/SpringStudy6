<?xml version="1.0" encoding="UTF-8"?>
<beans xmlns="http://www.springframework.org/schema/beans"
	xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
	xmlns:context="http://www.springframework.org/schema/context"
	xmlns:mybatis-spring="http://mybatis.org/schema/mybatis-spring"
	xsi:schemaLocation="http://mybatis.org/schema/mybatis-spring http://mybatis.org/schema/mybatis-spring-1.2.xsd
		http://www.springframework.org/schema/beans https://www.springframework.org/schema/beans/spring-beans.xsd
		http://www.springframework.org/schema/context http://www.springframework.org/schema/context/spring-context-4.3.xsd">
	
	<!-- Root Context: defines shared resources visible to all other web components -->
	<!-- 프로젝트 관련 스프링 설정의 정보(객체)를 가지고 있는 파일 -->
	<!-- 웹 정보를 제외한 모든 정보를 가지고 있음 -->
	
	<!-- DataSource DB연결 정보를 저장하는 객체 -->
	<bean id="dataSource"
		  class="org.springframework.jdbc.datasource.DriverManagerDataSource">
		  
		  <property name="driverClassName" value="com.mysql.cj.jdbc.Driver"/>
		  <property name="url" value="jdbc:mysql://localhost:3306/springdb"/>
		  <property name="username" value="root"/>
		  <property name="password" value="1234"/>
		  
  	</bean>
	<!-- DataSource DB연결 정보를 저장하는 객체 -->
	
	<!-- SqlSessionFactory : connection 생성(+연결정보), SQL쿼리 실행, Mybatis 접근 -->	
	<bean id="sqlSessionFactory" 
		  class="org.mybatis.spring.SqlSessionFactoryBean">
			
			<!-- <property name="dataSource" /> == setDataSource() -->
			<property name="dataSource" ref="dataSource"/>
			
<!-- 			classpath:/ 는 src/main/resources 폴더를 의미  -->
			<property name="configLocation" 
			          value="classpath:/mybatis-config.xml"/>
			<property name="mapperLocations"
			          value="classpath:/mappers/**/*Mapper.xml" />
	
	</bean>
	<!-- SqlSessionFactory : connection 생성(+연결정보), SQL쿼리 실행, Mybatis 접근 -->	
	
	<context:component-scan base-package="com.itwillbs.persistence"/>
	
	
	
	
	
	
</beans>
